import { EventUpdateBusiness } from './event-update-business';

describe('EventUpdateBusiness', () => {
  it('should create an instance', () => {
    expect(new EventUpdateBusiness()).toBeTruthy();
  });
});
