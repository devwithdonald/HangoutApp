import { BusinessEmployeeUser } from './business-employee-user';

describe('BusinessEmployeeUser', () => {
  it('should create an instance', () => {
    expect(new BusinessEmployeeUser()).toBeTruthy();
  });
});
